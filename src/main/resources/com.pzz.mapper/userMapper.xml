<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--映射，namespace命名空间，不能重复-->
<mapper namespace="com.pzz.dao.UserDao">
    <cache/>
    <select id="findAllCache" resultType="com.pzz.entity.User">
        select * from user
    </select>
</mapper>